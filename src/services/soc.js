// NO TOUCHY!!
/*
let io
exports.socketConnection = (server) => {
  io = require('socket.io')(server)
  io.on('connection', (socket) => {
    socket.join('some room')
  })
}

exports.getNumPlayers = () => {
  io.sockets.adapter.rooms.get('some room')
} */
